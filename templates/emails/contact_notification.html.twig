{% extends 'emails/_base.html.twig' %}

{% block title %}Nouveau message depuis le site{% endblock %}

{% block content %}
    <h2 style="font-size: 20px; color: #FF6B00; margin-top: 0;">Nouveau Message de Contact</h2>
    <p>Un nouveau message a été envoyé depuis le formulaire de contact du site.</p>
    <table width="100%" border="0" cellspacing="0" cellpadding="0" style="margin: 20px 0;">
        <tr>
            <td style="padding: 10px; background-color: #f4f4f7; border-radius: 5px;">
                <p style="margin: 0;"><strong>Nom :</strong> {{ name }}</p>
                <p style="margin: 5px 0 0 0;"><strong>Email :</strong> <a href="mailto:{{ email }}" style="color: #FF6B00;">{{ email }}</a></p>
            </td>
        </tr>
    </table>
    <p><strong>Message :</strong></p>
    <div style="border-left: 3px solid #FF6B00; padding-left: 15px; margin-left: 5px; color: #555555; background-color: #fdfdfd; border-radius: 5px;">
        <p><em>{{ message|nl2br }}</em></p>
    </div>
{% endblock %} 