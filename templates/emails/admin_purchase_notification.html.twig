{% extends 'emails/_base.html.twig' %}

{% block title %}Nouvelle Vente Réalisée !{% endblock %}

{% block content %}
    <h2 style="font-size: 20px; color: #FF6B00; margin-top: 0;">🎉 Nouvelle Vente !</h2>
    <p>Une nouvelle vente a été réalisée sur le site.</p>
    
    <h3 style="border-bottom: 2px solid #eee; padding-bottom: 10px; margin-top: 30px;">Détails du Véhicule</h3>
    <p><strong>Modèle :</strong> {{ order.vehicule.brand }} {{ order.vehicule.model }}</p>
    <p><strong>Année :</strong> {{ order.vehicule.year|date('Y') }}</p>
    <p><strong>Prix de vente :</strong> {{ (order.vehicule.salePrice / 100)|number_format(2, ',', ' ') }} €</p>
    
    <h3 style="border-bottom: 2px solid #eee; padding-bottom: 10px; margin-top: 30px;">Détails de l'Acheteur</h3>
    <p><strong>Nom :</strong> {{ order.users.firstname }} {{ order.users.lastname }}</p>
    <p><strong>Email :</strong> <a href="mailto:{{ order.users.email }}" style="color: #FF6B00;">{{ order.users.email }}</a></p>
    <p><strong>Téléphone :</strong> {{ order.users.phoneNumber }}</p>
    
    <h3 style="border-bottom: 2px solid #eee; padding-bottom: 10px; margin-top: 30px;">Détails de la Commande</h3>
    <p><strong>Numéro de commande :</strong> #{{ order.id }}</p>
    <p><strong>Date de la commande :</strong> {{ order.orderDate|date('d/m/Y à H:i') }}</p>
    
    <p style="text-align: center; margin-top: 30px;">
        <a href="{{ url('app_orders_show', {'id': order.id}) }}" style="background-color: #FF6B00; color: #ffffff; padding: 12px 25px; text-decoration: none; border-radius: 5px; font-weight: bold; display: inline-block;">
            Voir la commande dans l'admin
        </a>
    </p>
{% endblock %} 