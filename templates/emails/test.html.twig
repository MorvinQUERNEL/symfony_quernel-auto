<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Email de Test - Quernel Auto</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #000000;
            padding: 20px;
            text-align: center;
        }
        .header h1 {
            color: #FF6B00;
            margin: 0;
        }
        .content {
            padding: 20px;
            background-color: #ffffff;
        }
        .footer {
            background-color: #000000;
            color: #ffffff;
            text-align: center;
            padding: 20px;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>QUERNEL AUTO</h1>
        </div>
        <div class="content">
            <h2>Test de Configuration Email</h2>
            <p>{{ testMessage }}</p>
            <p>Date et heure du test : {{ date|date('d/m/Y H:i:s') }}</p>
            <p>Si vous voyez cet email dans MailHog, cela signifie que la configuration du mailer fonctionne correctement !</p>
        </div>
        <div class="footer">
            <p>© {{ "now"|date("Y") }} Quernel Auto - Tous droits réservés</p>
        </div>
    </div>
</body>
</html> 